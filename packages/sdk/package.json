{
  "name": "@appostolic/sdk",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist openapi.json",
    "gen": "openapi -i openapi.json -o src --client fetch --useUnionTypes true",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {
    "openapi-typescript-codegen": "^0.29.0",
    "rimraf": "^5.0.7",
    "typescript": "^5.5.4"
  }
}
