/**
 * Deprecated component preserved only as a no-op stub to avoid breaking deep imports
 * during migration to server-only TopBar gating. Will be removed entirely once all
 * external references are confirmed gone.
 */
export function TenantAwareTopBar() {
  return null;
}
export default TenantAwareTopBar;
