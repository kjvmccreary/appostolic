/**
 * (Intentionally empty)
 * Legacy TenantAwareTopBar tests removed after migrating to server-only TopBar gating.
 * File retained temporarily to avoid flaky deletions in automated environment.
 */

export {}; // silence isolatedModules / ensure file is treated as a module
